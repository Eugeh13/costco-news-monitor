[phases.setup]
nixPkgs = ['python311', 'gcc']

[phases.install]
cmds = [
    'pip install --upgrade pip',
    'pip install -e .',
]

[phases.build]
cmds = ['echo "Build complete for costco-news-monitor v2"']

[start]
cmd = 'python3.11 scheduler.py'
